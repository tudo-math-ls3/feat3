<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- global project properties -->
  <PropertyGroup Label="Globals">
    <Keyword>Win32Proj</Keyword>
    <RootNamespace>$(FeastAppName)</RootNamespace>
  </PropertyGroup>

  <ItemDefinitionGroup>
    <Link>
      <SubSystem>Console</SubSystem>
    </Link>
  </ItemDefinitionGroup>
  <PropertyGroup Label="Configuration">
    <ConfigurationType>Application</ConfigurationType>
  </PropertyGroup>

  <!-- import common properties -->
  <Import Project="vc12-common-props.xml" />

  <!-- ********************************************************************* -->
  <!-- Compiler / Linker Properties -->
  <!-- ********************************************************************* -->

  <!-- set output path -->
  <PropertyGroup>
    <OutDir>$(FeastBinPath)\</OutDir>
  </PropertyGroup>

  <!-- add link dependencies -->
  <ItemDefinitionGroup>
    <Link>
      <AdditionalLibraryDirectories>$(FeastLibDirList)</AdditionalLibraryDirectories>
      <AdditionalDependencies>$(FeastLibDepList)</AdditionalDependencies>
    </Link>
  </ItemDefinitionGroup>

  <!-- copy CUDA runtime -->
  <!--
  <ItemDefinitionGroup Condition="'$(HaveCUDA)'=='yes' And '$(BackendCUDA)'=='true'">
    <PostBuildEvent>
      <Command>copy "$(CudaToolkitBinDir)\cudart*.dll" "$(OutDir)"</Command>
    </PostBuildEvent>
  </ItemDefinitionGroup>-->

  <!-- import common targets -->
  <Import Project="vc12-common-options.xml" />

</Project>
